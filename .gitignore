.DS_Store
*.log
node_modules/

# Local test files
/tmp/

# ── Credentials & Secrets ──────────────────────────────────
# NEVER commit credentials — each team member uses their own
.env
.env.local
*.env
credentials.json
gcp-oauth.keys.json
**/secrets/

# macOS
.DS_Store
**/.DS_Store

# IDE
.idea/

# ── Regenerable Cache ──────────────────────────────────────
# TestRail cache is rebuilt on first /qa:fetch-testrail — don't track
testrail-cache/

# ── Playwright Test Artifacts ──────────────────────────────
test-results/
